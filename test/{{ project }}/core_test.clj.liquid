(ns {{ project }}.core-test
  (:require [clojure.test :refer :all]
            [{{ project }}.core :refer :all]))
(ns {{ project }}.core-test
  (:use [{{ project }}.core])
  (:use [midje.sweet]))

(facts "about failing tests"
  (fact "they fail"
    (= 0 1) => true ))
